apiVersion: v1
kind: Service
metadata:
  name: {{ include "cooccurrence.fullname" . }}
  labels:
    {{- include "cooccurrence.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cooccurrence.selectorLabels" . | nindent 4 }}
